<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.unistuttgart.ipvs.pmp"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk android:minSdkVersion="7" />

    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:icon="@drawable/logo"
        android:label="@string/app_name" >
        <uses-library android:name="com.google.android.maps" />

        <!-- Activities can be declared here -->
        <activity
            android:icon="@drawable/logo"
            android:label="vHike"
            android:name=".apps.vhike.gui.LoginActivity"
            android:theme="@android:style/Theme.NoTitleBar" >
            <intent-filter >
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".apps.vhike.gui.MainActivity"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity
            android:name=".apps.vhike.gui.RideActivity"
            android:theme="@android:style/Theme.NoTitleBar" />
        <activity android:name=".apps.vhike.gui.DriverViewActivity" />
        <activity android:name=".apps.vhike.gui.PassengerViewActivity" >
        </activity>
        <activity
            android:name=".apps.vhike.gui.HistoryActivity"
            android:theme="@android:style/Theme.NoTitleBar" />
    </application>

</manifest>