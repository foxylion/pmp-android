<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
      package="de.unistuttgart.ipvs.pmp"
      android:versionCode="1"
      android:versionName="1.0">
    <uses-sdk android:minSdkVersion="7" />

	<!-- The android:name ensures that the Application is loaded properly -->
    <application android:icon="@drawable/icon" android:label="@string/app_name" android:name="de.unistuttgart.ipvs.pmp.PMPApplication">
    
    	<!-- Activities can be declared here -->
    	
    	<!-- // end of Activity declaration -->
    
    	<!-- The PMPService can be loaded by using the intent action name declared 3 lines below -->
        <service android:name=".service.PMPService" android:exported="true">
            <intent-filter>
                <action android:name="de.unistuttgart.ipvs.pmp.PMPService"></action>
            </intent-filter>
        </service>
    </application>
</manifest>