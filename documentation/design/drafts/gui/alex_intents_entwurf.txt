

Methoden des Services:
//Sagt dem PMPService, er solle die Activity Apps zeigen. 
//Der Service ruft die Activity Apps auf und sendet einen Intent mit bestimmten
//Daten zu. // Intent muss noch aufgebaut werden.
Intent sollte folgende Daten beinhalten:
-Liste aller Apps
für jede App:
	-Name
	-ID der App
	-Version
	-Beschreibung
//	-Zugewiesene Presets
	-zugewiesene ServiceLevel
	-angeboteneServiceLevel
	-Instalations Datum
Der Service bietet in diesem Zusammenhang folgende Funktionen an:
	//public void assignPreset(List<Preset> presets, int IDderApp);
	//public void removeAssignment(Preset preset, int IDderApp);
	public void assignServiceLevel(int IDdesLevels, int IDderApp);
	public void removeApp(IDderApp);
showAppActivity();

//Sagt dem PMPService, er solle die Activity Ressources zeigen. 
//Der Service ruft die Activity Ressources auf und sendet einen Intent mit bestimmten
//Daten zu. //Intent muss noch aufgebaut werden.
Intent sollte folgende Daten beinhalten:
verfügbare Ressourcen-Gruppen
für jede Gruppe
	- Name
	- Beschreibung
	- zugehörige Ressourcen
	- Version
Privacy Levels, die diese Ressourcen-Gruppe beinhaltet
Der Service bietet in diesem Zusammenhang folgende Funktionen an:
	public void removeResGroup(IDderGruppe);
	public void installResGroup(RessourceGroup);
showResActivity();

//Sagt dem PMPService, er solle die Activity Presets zeigen. 
//Der Service ruft die Activity Presets auf und sendet einen Intent mit bestimmten
//Daten zu. //Intent muss noch aufgebaut werden.
Intent:
	- Name des Presets
	- Beschreibung ?
	- Apps die zur diesem Presset gehören

Der Service bietet folgende Funktionen an:
	public void newPreset();
	public void removePreset(IDdesPreset);
	public void editPreset(IDdesPreset);
	public void renamePreset(); // Wobei Edit es schon abdeckt.
ShowPresetActivity();

//Sagt dem PMPService, er solle die Activity Mülleimer zeigen. 
//Der Service ruft die Activity Mülleimer auf und sendet einen Intent mit bestimmten
//Daten zu. //Intent muss noch aufgebaut werden.
Intent:
	- Liste der gelöschten Presets
Funktionen:
	public void recoverItem(IDdesItems);
	public void clearAll(); oder clearSelected(IDdesItems);
ShowTrashActivity();
