Ist-Analyse:
Bestehende Möglichkeiten sind begrenzt, z.B. „SMS-Versenden nicht erlauben“ verhindert App-Installation.
Durch mehr Rechte wird App besser.
Bsp.: Schach-App kann durch „Internet-Recht“ Highscore synchronisieren.

Platform auf Android aufsetzen, da wir das System nicht mehr verändern können.

Services von Android erweitern, nicht ersetzen.
Bsp.: Matrikel-Nummer von Studenten durch Web-Service abrufen, der nur durch unsere API abrufbar sein soll
kein Root-Recht soll erforderlich sein
Es muss nicht Android-Rechte durchschleifen, aber soll dafür erweitert werden können
domänespezifisch: Ort im Gebäude (granular regulierbar)
allgemein: schreiben/lesen/anlegen/ändern/nicht ändern/ausführen/sms verschicken/gps einschalten/gps nutzen (nur wenns schon aktiv ist)/...
neue Möglichkeiten müssen integrierbar sein, ohne Code Änderungen

Wer soll es bedienen?
Nicht-Informatiker (also auch die Oma soll es bedienen können) - "leicht beherschbar" und trotzdem alle Mächtigkeit (Einfach-/Expertenmodus)
Programmierer: will level einfach definieren können
Platformmenschen: Standardressourcen

Einstellungen sollen austauschbar sein

Firmenaspekt (Zugriffsrechte nur vom Admin änderbar) vorerst nicht

Muss auf den Geräten funktionieren die zur Verfügung gestellt werden (wahrscheinlich mind. 2.1.1)

GUI sollte gut sein, gut zu bedienen sein, wichtig sind aber die Konzepte!

Linzenz relativ egal (Uni uneingeschränktes Nutzungsrecht)

Übliche Java-Konventionen

XACML für das austauschbare Format (wäre schön) <-- sollte sich jemand aneignen und durcharbeiten

Kostenpflichtige Sachen müssen gefragt werden

Lizenz-kompatible Bibliotheken dürfen eingebunden werden (bei exotischen Sachen fragen)

Continous Intergration (nach jedem Cycle muss eine Lauffähige Version verfügbar sein)

Serverseitig am Anfang nicht nötig (spätere Iteration)
Domänespezifisch (auch eher später, nur anfangs im Modell angelegt)
7. kann später kommen
Levels of Privacy sollen ganz früh kommen
Oberfläche soll früh gut benutzbar sein (zentraler Aspekt)
	Zumindest der einfache modus, der expertenmodus kann auch erst später kommen (knopf soll trotz "unfertiges produkt"-aspket schon da sein)
	Möglichst schnell komplett

Server soll beispielhaft implementiert werden

Spezifikation nur für eine Iteration schreiben.

Bsp.: Anfahrtsskizze (keine Ortung: Übersicht; Stadt: Autobahn XY; Straße: Routenvorschlag; GPS (dauerhaft): Navigation)

Für Beispiel-App gibt es zum späteren Zeitpunkt noch Use-Cases

Service-Level-Editor erstmal nicht (Definition der Service-Levels muss aber einfach möglich sein)

Englisch = GUI-Sprache (erweiterbar)

Funktionalität sollte Dokumentiert werden ("Benutzerhandbuch", in Deutsch)

Schrift & Co Android konform (sonst keine Anforderungen)

Anforderungen an die Funktionialität sind nicht hart vorhanden (So wie die Android-API)

Anwendung kann Resourcen-Typ definieren, den andere Apps dann nutzen können müssen.

Keine Anforderungen and die Robustheit (aber soll interaktiv benutzbar sein)

Barierefreiheit muss nicht speziell gegeben sein

PMP = Name der App

2 Handy werden auf jeden Fall zur Verfügung gestellt.

Email, oder vorbei kommen.

Ende nächster Woche soll Angebot stehen

Angebot inkl. Modell (ganz Abstrakt, das kann das, der darf dass, bekommt das von dem, ...)

Angebot inkl. der Iterationen (Inhalte derer)

Grobe Zeitplanung

"Mengengerüst:"
10 Punkte für die Resourcen
8 Variationen pro Punkt (Service-Level)

Bedingungen für Zugriffe auf Resourcen, soll Modular Erweiterbar sein